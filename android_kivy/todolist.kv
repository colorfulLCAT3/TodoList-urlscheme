# TodoList Kivy KV 样式定义
# 主界面逻辑在 todolist_app.py（Python 构建 UI），此文件仅提供全局样式

<Widget>:
    font_name: 'DroidSansFallback'

<Label>:
    font_size: sp(16)

<Button>:
    font_size: sp(15)

<TextInput>:
    font_size: sp(15)

<Spinner>:
    font_size: sp(15)
