# AndroidX 支持
android.useAndroidX=true
# 自动迁移非 AndroidX 依赖（本项目无需，但保留无妨）
android.enableJetifier=true

# Kotlin code style
kotlin.code.style=official

# Gradle JVM 内存
org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
